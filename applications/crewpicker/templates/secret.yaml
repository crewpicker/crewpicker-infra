apiversion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "labels" . | nindent 4 }}
type: Opaque
data:
  {{- $r := .Release.Name -}}
  DATABASE_URL: {{ .Values.$r-database-url | b64enc | quote }}
